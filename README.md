# python-flask-basics

```bash
cd myproject
# crea un ambiente virtual para aislar las dependencias del proyecto vs las instaladas en el sistema
python3 -m venv venv
# activa el ambiente virtual
. venv/bin/activate
pip3 install Flask
```
